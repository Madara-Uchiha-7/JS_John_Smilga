let name;
// if you write a variable without initialzing the value then it is called
// as declaring the variable.
// if you use name without initialzing the value to it then you will
// following o/p in console:
// undefined
console.log(name);

// let variableName = ""; // this is called as empty string

let address, zip, state;
address = "warje";
zip = "idk";
state = "MH";

// we can print the variable on the same line too using ,
console.log(address, zip, state);
